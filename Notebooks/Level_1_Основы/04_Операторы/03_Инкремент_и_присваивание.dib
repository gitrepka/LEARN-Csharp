#!meta

{"kernelInfo":{"defaultKernelName":"csharp","items":[{"aliases":[],"name":"csharp"}]}}

#!markdown

# Инкремент, декремент и сокращённое присваивание
> **Уровень:** 1 — Основы | **Тема:** Операторы (3/3)
> **Предыдущий:** [02_Сравнение_и_логика](02_Сравнение_и_логика.dib) | **Следующий:** [05_Условия / 01_If_Else](../05_Условия/01_If_Else.dib)
---

## Зачем это нужно?

В программировании очень часто нужно увеличить или уменьшить переменную на 1 (счётчик цикла, количество жизней) или на определённую величину (добавить золото, уменьшить здоровье). Для этого есть удобные сокращения.

---

## 1. Инкремент и декремент

`++` увеличивает на 1, `--` уменьшает на 1. Есть **префиксная** и **постфиксная** форма.

#!csharp

int счётчик = 0;

счётчик++;   // Было 0, стало 1
Console.WriteLine($"После счётчик++: {счётчик}");

счётчик++;   // Было 1, стало 2
Console.WriteLine($"После ещё ++: {счётчик}");

счётчик--;   // Было 2, стало 1
Console.WriteLine($"После счётчик--: {счётчик}");

Console.WriteLine();

// Префикс vs Постфикс — разница при использовании в выражении:
int a = 5;
int b = a++;   // СНАЧАЛА b получает значение a (5), ПОТОМ a увеличивается
Console.WriteLine($"a++ → a={a}, b={b}");   // a=6, b=5

int c = 5;
int d = ++c;   // СНАЧАЛА c увеличивается, ПОТОМ d получает значение
Console.WriteLine($"++c → c={c}, d={d}");   // c=6, d=6

// Совет: для простоты используй ++ и -- отдельной строкой, не в выражениях

#!markdown

## 2. Сокращённые операторы присваивания

Вместо `x = x + 5` можно писать `x += 5`. Работает со всеми арифметическими операторами.

#!csharp

int золото = 100;
Console.WriteLine($"Начальное золото: {золото}");

золото += 50;    // золото = золото + 50
Console.WriteLine($"Нашёл сундук (+50): {золото}");   // 150

золото -= 30;    // золото = золото - 30
Console.WriteLine($"Купил зелье (-30): {золото}");     // 120

золото *= 2;     // золото = золото * 2
Console.WriteLine($"Удвоение (*2): {золото}");          // 240

золото /= 3;     // золото = золото / 3
Console.WriteLine($"Разделил на троих (/3): {золото}"); // 80

золото %= 7;     // золото = золото % 7
Console.WriteLine($"Остаток от 7 (%7): {золото}");      // 3

#!markdown

---

## 🟢 Упражнение

Напиши симуляцию «Игровой магазин»:
1. Создай переменную `золото = 500`
2. Создай переменную `покупки = 0`
3. «Купи» 3 предмета:
   - Меч за 150 (золото -= 150, покупки++)
   - Щит за 100 (золото -= 100, покупки++)
   - Зелье за 25 (золото -= 25, покупки++)
4. После каждой покупки выведи: "Купил [предмет]! Золото: {золото}, покупок: {покупки}"
5. В конце используй `*=` чтобы удвоить оставшееся золото (бонус!) и выведи итог

#!csharp

// Твой код здесь:

#!markdown

---

## ❓ Контрольные вопросы
1. Чем отличается `a++` от `++a`?
2. Что означает запись `x += 10`? Перепиши её в полную форму.
3. Какие сокращённые операторы присваивания существуют?

---

> **Следующий →** [05_Условия / 01_If_Else](../05_Условия/01_If_Else.dib)
