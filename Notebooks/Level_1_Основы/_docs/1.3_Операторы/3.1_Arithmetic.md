# 3.1. Арифметические операторы, тернарный, приоритет

## Что ты узнаешь
- Арифметические операторы (+, -, *, /, %, ++, --)
- checked/unchecked для переполнения
- Тернарный оператор
- Приоритет операторов

> Сравнение → 3.2, Логика → 3.3, Биты → 3.4, Null-операторы → 3.5

---

## ЗАЧЕМ?
Типы данных — это ингредиенты. Операторы — это рецепты. Без операторов ты не можешь ничего СДЕЛАТЬ с данными.

## Арифметические операторы

```csharp
int a = 10, b = 3;

Console.WriteLine(a + b);   // 13 — сложение
Console.WriteLine(a - b);   // 7  — вычитание
Console.WriteLine(a * b);   // 30 — умножение
Console.WriteLine(a / b);   // 3  — целочисленное деление! (не 3.33)
Console.WriteLine(a % b);   // 1  — остаток от деления (10 / 3 = 3, остаток 1)

// Для дробного деления хотя бы один операнд должен быть дробным:
Console.WriteLine(10.0 / 3);   // 3.3333...
Console.WriteLine((double)a / b); // 3.3333... — приведение типа
```

**Инкремент и декремент:**
```csharp
int x = 5;

// Постфиксный: СНАЧАЛА использует, ПОТОМ увеличивает
Console.WriteLine(x++); // Выведет 5, потом x станет 6

// Префиксный: СНАЧАЛА увеличивает, ПОТОМ использует
Console.WriteLine(++x); // x сначала станет 7, выведет 7
```

**checked / unchecked — контроль переполнения:**
```csharp
int max = int.MaxValue; // 2 147 483 647

// По умолчанию — тихо переполнится!
int overflow = max + 1; // -2 147 483 648 (???)

// checked — кинет исключение при переполнении
checked
{
    int safe = max + 1; // OverflowException!
}
```

## Тернарный оператор

```csharp
int health = 30;
string status = health > 50 ? "Здоров" : "Ранен";
// Читается: "если health > 50, то 'Здоров', иначе 'Ранен'"

// В Godot:
float speed = isRunning ? 200f : 100f;
```

## Приоритет операторов (от высокого к низкому)

| Приоритет | Операторы |
|-----------|-----------|
| 1 (высший) | `x++`, `x--`, `?.`, `?[]` |
| 2 | `!`, `~`, `++x`, `--x`, приведение `(T)` |
| 3 | `*`, `/`, `%` |
| 4 | `+`, `-` |
| 5 | `<<`, `>>` |
| 6 | `<`, `>`, `<=`, `>=`, `is`, `as` |
| 7 | `==`, `!=` |
| 8 | `&` |
| 9 | `^` |
| 10 | `\|` |
| 11 | `&&` |
| 12 | `\|\|` |
| 13 | `??` |
| 14 | `? :` (тернарный) |
| 15 (низший) | `=`, `+=`, `-=`, `*=` и т.д. |

**Совет:** Если не уверен — ставь скобки! `(a + b) * c` понятнее чем `a + b * c`.

---

## Частые ошибки

### 1. Целочисленное деление
```csharp
// ❌ Ожидал 3.33, получил 3
int result = 10 / 3;

// ✅ Приведи к double:
double result = 10.0 / 3;
```

### 2. = вместо ==
```csharp
// ❌ Присваивание, не сравнение!
if (x = 5) { }  // Ошибка компиляции в C#

// ✅ Двойное равно для сравнения:
if (x == 5) { }
```

### 3. Путаница prefix/postfix
```csharp
int a = 5;
Console.WriteLine(a++ + ++a);
// a++ → использует 5, потом a=6
// ++a → a=7, использует 7
// 5 + 7 = 12
// Лучше НЕ писать такой код — разбивай на строки!
```

---

## Мини-упражнения

1. **⭐ Что выведет?** `int a = 5; Console.WriteLine(a++ + ++a);` — Объясни пошагово.
2. **⭐** Используй `checked` блок, вызови переполнение `int.MaxValue + 1`.
3. **⭐** Напиши тернарный оператор: если число чётное — "Чёт", иначе "Нечет". Подсказка: `%`.
4. **⭐** Предскажи результат, потом проверь: `2 + 3 * 4`, `(2 + 3) * 4`, `true || false && false`.

---

## Практика Uno Platform ⭐⭐

**"Умный калькулятор"**
Полноценный калькулятор с UI: кнопки цифр (0-9), операции (+, -, *, /), дисплей TextBlock. Обработка деления на ноль.

## Практика Godot ⭐⭐

**Система урона**
`finalDamage = (baseDamage * multiplier) - armor`
- Критический удар через тернарный оператор
- Отображай расчёт на экране через Label

---

## Контрольные вопросы

1. Чем отличается `/` для int и для double?
2. Зачем нужен `checked`?
3. Что будет: `true || false && false`? Почему?

---
*Подробнее: сравнение → 3.2, логика → 3.3, биты → 3.4, null-операторы → 3.5*
